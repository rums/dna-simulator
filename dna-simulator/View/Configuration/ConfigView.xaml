<UserControl x:Class="dna_simulator.View.Configuration.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ignore="http://www.ignore.com"
             xmlns:configuration="clr-namespace:dna_simulator.View.Configuration"
             mc:Ignorable="d ignore">
    <Border BorderBrush="Gray" BorderThickness="1" Margin="10" Padding="10">

        <Grid x:Name="LayoutRoot" Margin="-2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <configuration:SingleTileControl DataContext="{Binding CurrentSingleTileViewModel}" />
            <configuration:MultiTileControl Grid.Column="1" Width="Auto" DataContext="{Binding CurrentMultiTileViewModel}" />
        </Grid>
    </Border>
</UserControl>